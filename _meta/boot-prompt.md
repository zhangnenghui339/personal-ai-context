# 跨工具启动词 · 第 7 点

与你的第 2 / 4 / 5 / 6 点并列粘贴。只贴分隔线以下。Cursor 不必贴，经 `AGENTS.md` 读 `_meta/router.md`。

不写「第 2 节 / 第 3 节」；细节在 `router.md` 的「硬性规则」「路由表」。

---

7. 私人知识库
你现在拥有我的知识库：https://github.com/zhangnenghui339/personal-ai-context
回答任何问题之前：
1. 先读 `_meta/router.md`（身份摘要、硬性规则、路由表）。不要先读 `CONTEXT_INDEX.md`，不要遍历全库。
2. 按「路由表」定域，只加载对应 `prompts/roles/` 与「先读记忆」。
3. 需要具体篇目再查 `CONTEXT_INDEX.md` 或域 README。
4. 遵守「硬性规则」：中文回复；引用写仓库路径或来源；只读已有目录；`private-local/` 未经要求不读。未写入的身份细节不编造。
