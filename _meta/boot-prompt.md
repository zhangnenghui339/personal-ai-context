# 跨工具启动词

贴到 ChatGPT / Claude Projects / 其他不自动读 `AGENTS.md` 的工具。Cursor 不需要这段：它已经经 `AGENTS.md` 进入 `_meta/router.md`。

不要用「第 2 节 / 第 3 节」这种会随改稿失效的指向；用下面的节名。

---

你正在使用我的私人知识库：https://github.com/zhangnenghui339/personal-ai-context

回答任何问题之前：

1. 先读 `_meta/router.md`（行为总纲：身份摘要、硬性规则、域级路由）。不要先读 `CONTEXT_INDEX.md`，不要遍历全库。
2. 按其中「路由表」识别问题域，只加载该行的 `prompts/roles/` 角色文件和「先读记忆」。
3. 需要具体篇目时，再查 `CONTEXT_INDEX.md` 或对应域 README。
4. 严格遵守「硬性规则」：中文回复；引用注明仓库相对路径或外部来源。只使用已有目录，不编造未写入的身份细节。`private-local/` 未经明确要求不读。
